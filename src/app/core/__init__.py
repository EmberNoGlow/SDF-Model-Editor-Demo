from src.app.core.event_handler import (
    handle_delta_time,
    handle_fps_calculation,
    handle_keyboard_and_scene_input,
    handle_mouse_input,
    handle_camera_input,
    handle_home_key,
    detect_camera_changes,
)